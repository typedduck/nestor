# webapp configuration

[server]
port = {{.Port}}
log_level = "{{.LogLevel}}"

[database]
host = "{{.DBHost}}"
port = {{.DBPort}}
name = "{{.DBName}}"

[redis]
host = "{{.RedisHost}}"
port = {{.RedisPort}}
